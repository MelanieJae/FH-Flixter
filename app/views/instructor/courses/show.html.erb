<br />
<div class="frosty col-xs-10 col-xs-offset-1">
	<h1 class="text-center"><%= @course.title %></h1>
	<br />
	<%= link_to 'Preview as Student', course_path(@course), :class => 'btn btn-warning pull-right' %>
	<div class="text-center">
		<%= link_to 'Add a new section', new_instructor_course_section_path(@course), :class => "btn btn-warning pull-right" %>
	</div>
	
	<ul>
		<% @course.sections.each do |section| %>
			<li>
				<%= section.title %></div>
			</li>
		<% end %>
	</ul>
</div>

	