<br />
<div class="main-box-gray col-xs-10 col-xs-offset-1">
  <h1>Add a new lesson</h1>
  <%= simple_form_for @lesson, :url => instructor_section_lessons_path(@section) do |f| %>
  	<%=	f.input :title %>
  	<%= f.input :subtitle %>
  	<%= f.submit "Add lesson", :class => 'btn btn-warning' %>
  	<br />
  	<br />
  <% end %>
</div>

